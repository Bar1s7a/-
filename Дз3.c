#include<stdlib.h>
#include<stdio.h>

int main(){
	int a, b, s = 1, i = 1;
	scanf("%d %d", &a, &b);
	while(i < = b){
		s = s * a;
		i++;
}
	printf("%d -> %d\n", a, s);
	system("pause");
	return 0;
}
